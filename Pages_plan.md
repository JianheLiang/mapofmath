**Homepage**

From top to bottom, three functional areas arranged sequentially. 
1. Search Tab (Block A: navigate to *search results page* (Page A))
2. Exploring Connection (Block B: navigate to *grand connection graph page*(Page B))
3. Exploring Chronological Timeline (Block C: navigate to *grand timeline page*(Page C))
Do not use rectangular block to separate each block. 

*Note for Block B and C*: these two block should just be a preview of the real pages: they are dynamic and interactive(i.e. movable), but when the user click on any part of it, directed to the corresponded page.

**Wikipage**
Each data(concepts/theorem/mathematician) in database has its own (separate)wiki page. Access through Page A/Page B/Page C. Contain the following:
1. Content and Reference(Imported)
	1. Content includes the general introduction, details(explanations for a concept, proof for a theorem, bibliography for a mathematician), and references(hyperlink)
2. Historical Timeline (rendered according to the time data)
3. Connections to other (rendered according the links in the original data file)

**Grand Connection Graph (Page B)**
1. Connection
2. Compactification: 

**Grand Timeline (Page C)**



