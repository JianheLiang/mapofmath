**Homepage**

From top to bottom, three functional areas arranged sequentially. 

1. Search Tab (Block A: navigate to *search results page* (Page A))
2. Exploring Connection (Block B: navigate to *grand connection graph page*(Page B))
3. Exploring Chronological Timeline (Block C: navigate to *grand timeline page*(Page C))

*Note for Block A*: Do not show any wikis before search, on the main page, there should be only search tab and filter.

*Note for Block B and C*: these two block should just be a preview of the real pages(consistent with design for each page): they are dynamic and interactive(i.e. movable), but when the user click on any part of it, directed to the corresponded page.

**Wikipage**
Each data(concepts/theorem/mathematician) in database has its own (separate)wiki page. Access through Page A/Page B/Page C. Contain the following:

1. Content and Reference(Imported)
  1. (Current version is ok) Content includes the general introduction, details(explanations for a concept, proof for a theorem, bibliography for a mathematician), and references(hyperlink)
2. Historical Timeline (rendered according to the time data)
3. Connections (graph view) (rendered according the links in the original data file, only show direct links. If users click on nodes, jump to corresponded page)

**Grand Connection Graph (Page B)**
A similar (but narrower) search bar(with filters) on the left side of the page, but this time the search results should be displayed just below the search tab, and user click on the 

1. Connection
2. Compactification: At most 30 nodes can be shown simultaneously on the

**Grand Timeline (Page C)**
Horizontal timeline